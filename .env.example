# Orchestrator Server Configuration
# Copy this file to .env and adjust values for your environment

PORT=3001
DATA_DIR=./data
TMUX_SOCKET=/tmp/orchestrator-tmux.sock

# Claude CLI path (leave empty to auto-detect from PATH, nvm, /usr/local/bin, /usr/bin)
CLAUDE_PATH=
